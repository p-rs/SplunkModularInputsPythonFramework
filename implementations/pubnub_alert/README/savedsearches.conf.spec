# Pubnub alert settings

action.pubnub = [0|1]
* Enable Pubnub alert

action.pubnub.param.channel = <string>
* The channel to send the message to
* (required)

action.pubnub.param.message = <string>
* The message to send 
* (required)
